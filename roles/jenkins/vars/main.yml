---
# Maps vault variables to role variables.
# Requires roles/jenkins/vars/vault.yml (ansible-vault encrypted).
jenkins_admin_password: "{{ vault_jenkins_admin_password }}"
gerrit_ssh_username: "{{ vault_gerrit_ssh_username }}"
gerrit_ssh_private_key: "{{ vault_gerrit_ssh_private_key }}"
