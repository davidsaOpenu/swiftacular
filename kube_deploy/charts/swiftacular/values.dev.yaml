swift:
  hashPathSuffix: swiftacular-dev-suffix
  hashPathPrefix: swiftacular-dev-prefix

grafana:
  enabled: true

secrets:
  keystoneAdminPassword: devpassword
  keystoneMysqlPassword: devmysqlpass
  keystoneGenericServicePassword: devservicepass
  # Must stay CHANGEME: the shared workload_tests/test_workload.py (also used
  # by the VM/Ansible flow) hardcodes KEY = "CHANGEME" for the achilles user.
  keystoneTestUserPassword: CHANGEME
  grafanaAdminPassword: devgrafanapass
