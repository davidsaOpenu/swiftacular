---
# Run workload tests and verification

- name: Workload tests
  hosts: package_cache
  become: true
  tasks:
    - name: Copy tests directory to package_cache host
      ansible.builtin.copy:
        src: workload_tests
        dest: /home/vagrant/
        mode: "0755"

    - name: Enable CRB repository
      ansible.builtin.command:
        cmd: dnf config-manager --enable crb
      changed_when: false

    - name: Install OpenStack Epoxy release package
      ansible.builtin.dnf:
        name: centos-release-openstack-epoxy
        state: present

    - name: Update package cache
      ansible.builtin.dnf:
        update_cache: true

    - name: Install pytest using system package manager
      ansible.builtin.dnf:
        name: python3-pytest
        state: present

    - name: Install python3-swiftclient
      ansible.builtin.dnf:
        name: python3-swiftclient
        state: present

    # We're currently only running the tiny test. It would be good to run everything!
    - name: Run workload tests
      ansible.builtin.command: /usr/bin/python -m pytest "test_workload.py::test_tiny_workload" "test_workload.py::test_large_object_workload" -svx
      args:
        chdir: /home/vagrant/workload_tests
      register: pytest_result
      changed_when: false

# - name: Verify sharding
#   hosts: storage
#   tasks:
#     # It's entirely possible that for bigger tests, it would take much longer than 10 minutes for sharding to complete.
#     - name: Wait for sharding
#       ansible.builtin.pause:
#         seconds: 600

#     # The container name in the command is hard-coded to match the workloader test. It would be good to make it variable!
#     - name: Get container sharding results
#       ansible.builtin.shell: |  # Big ugly command to check whether container is sharded.
#         set -o pipefail;
#         for FILE in /srv/node/td*/containers/*/*/*/*.db; do swift-manage-shard-ranges $FILE info; done 2>&1 \
#           | grep '^Loaded.*the-test-container$' -A 20 \
#           | grep -m 1 "db_state" \
#           | sed -e "s/^db_state = //"
#       register: command_output
#       changed_when: false

#     - name: Show output
#       ansible.builtin.debug:
#         var: command_output.stdout

#     - name: Check the output
#       ansible.builtin.fail:
#         msg: "The container is unsharded"
#       when: command_output.stdout == "unsharded"

#     - name: Verify sharded status
#       ansible.builtin.assert:
#         that:
#           - command_output.stdout == "sharded" or command_output.stdout == ""
#         fail_msg: "Host must be sharded or have no replica"
#         success_msg: "Sharded status verified"
